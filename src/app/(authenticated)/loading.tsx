import { Loading } from "@/components/ui/loading";

export default function DashboardLoading() {
  return <Loading variant="dashboard" fullScreen text="Loading Dashboard" subtitle="Preparing your workspace..." />;
}
