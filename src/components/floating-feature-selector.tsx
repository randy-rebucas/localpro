"use client";

/**
 * FloatingFeatureSelector - Deprecated
 * 
 * This component is now integrated into the GlobalHeader as an icon button.
 * Keeping this component for backward compatibility but it no longer renders anything.
 * The feature selector is now accessible via the header icon button.
 */
export function FloatingFeatureSelector() {
  // Component moved to GlobalHeader - no longer needed here
  // Keeping for backward compatibility in case it's referenced elsewhere
  return null;
}

